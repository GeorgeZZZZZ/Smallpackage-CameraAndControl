using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace GeorgeScript
{
    public class Unselectable_Unit_but_Hitable : MonoBehaviour
    {
        public GameObject targetCircle; // add projector to here after been target

        // Start is called before the first frame update
        void Start()
        {

        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}

